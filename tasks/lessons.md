# Lessons

- **Don't simplify the interface.** "Not a musician" does not mean "wants fewer controls" - the user
  understands formants, octave folding, range and the chord chart and has used them. Build on top of
  the existing layout and controls; never propose hiding them, replacing them with presets, or
  redesigning the page unless explicitly asked.
